`DLSS` and `minhook` are git submodules, pinned to the exact commits this
project builds against. After cloning:

```
git submodule update --init
```

Both are separately licensed (not GPL — see [EXCEPTIONS.md](../EXCEPTIONS.md)
at the repo root for how that's reconciled with this project's license).
